<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Full-screen, no action bar — the WebView fills the whole TV screen. -->
    <style name="Theme.PipTv" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
